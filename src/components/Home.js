export default function Home() {
  return (
    <div>
      <p style={{ padding: "15px", textAlign: "justify" }}>
        The idea is to provide the shortest route based on your shopping list.
        Based on the category of the products it will inform where to go first
        and so son.
      </p>
    </div>
  );
}
